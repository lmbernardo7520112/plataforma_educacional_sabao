{
  "buildCommand": "npm run build -w shared && cd client && npx vite build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://ecosabon-api.onrender.com/api/:path*" }
  ]
}
